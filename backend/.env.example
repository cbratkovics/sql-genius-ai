# Optional live generation. Keep server-side; never use a NEXT_PUBLIC_ prefix.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-haiku-20240307

# Account/database routes and startup initialization; use only an authorized database.
DATABASE_URL=
JWT_SECRET_KEY=
CORS_ORIGINS=http://localhost:3000
ENVIRONMENT=development
ENABLE_DOCS=true

# Optional/reference integrations; not required by the sample playground or generation route.
REDIS_URL=
